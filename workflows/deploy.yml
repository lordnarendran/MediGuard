# name: Deploy MediGuard Backend

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'backend/**'
#       - 'infra/template.yaml'
#       - '.github/workflows/deploy.yml'
#   workflow_dispatch:

# env:
#   AWS_REGION: us-east-1
#   SAM_STACK_NAME: mediguard-backend
#   SAM_S3_BUCKET: mediguard-sam-artifacts   # update to your S3 bucket for SAM artifacts

# jobs:
#   deploy:
#     name: Build & Deploy
#     runs-on: ubuntu-latest
#     permissions:
#       id-token: write   # required for OIDC auth
#       contents: read

#     steps:
#       # ── 1. Checkout ──────────────────────────────────────────
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       # ── 2. Configure AWS credentials ─────────────────────────
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#       # ── 3. Set up Python (matches Lambda runtime) ────────────
#       - name: Set up Python 3.12
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'

#       # ── 4. Set up AWS SAM CLI ────────────────────────────────
#       - name: Set up AWS SAM CLI
#         uses: aws-actions/setup-sam@v2
#         with:
#           use-installer: true

#       # ── 5. SAM Build ─────────────────────────────────────────
#       - name: SAM Build
#         working-directory: mediguard
#         run: sam build --template infra/template.yaml --build-dir .aws-sam/build

#       # ── 6. SAM Deploy ────────────────────────────────────────
#       - name: SAM Deploy
#         working-directory: mediguard
#         run: |
#           sam deploy \
#             --template-file .aws-sam/build/template.yaml \
#             --stack-name ${{ env.SAM_STACK_NAME }} \
#             --s3-bucket ${{ env.SAM_S3_BUCKET }} \
#             --region ${{ env.AWS_REGION }} \
#             --capabilities CAPABILITY_IAM \
#             --no-fail-on-empty-changeset \
#             --parameter-overrides \
#               BedrockRegion=${{ env.AWS_REGION }} \
#               BedrockModelId=${{ secrets.BEDROCK_MODEL_ID || 'global.anthropic.claude-sonnet-4-5-20250929-v1:0' }} \
#               MaxTokens=2000 \
#               StageName=prod

#       # ── 7. Print API endpoint ────────────────────────────────
#       - name: Print deployed API endpoint
#         working-directory: mediguard
#         run: |
#           echo "✅ Deployment complete."
#           echo "API Endpoint:"
#           aws cloudformation describe-stacks \
#             --stack-name ${{ env.SAM_STACK_NAME }} \
#             --region ${{ env.AWS_REGION }} \
#             --query "Stacks[0].Outputs[?OutputKey=='ApiEndpoint'].OutputValue" \
#             --output text
